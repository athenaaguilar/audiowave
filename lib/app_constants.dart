import 'package:flutter/material.dart';

const String appName = 'Audio Wave';

const appPrimaryDarkColor = Color(0xFF424242);
const appPrimaryLighColor = Color.fromARGB(255, 106, 105, 105);
const appSecondaryColor = Colors.white;
const disabledColor = Colors.grey;

const squareIcon = Icon(Icons.square_rounded, size: 25);
const roundIcon = Icon(Icons.mic_rounded, size: 40);
